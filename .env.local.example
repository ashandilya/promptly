# Google Service Account Credentials & Spreadsheet Info
# Replace with your actual values. Keep this file out of version control (.gitignore).
# You can get these credentials from the Google Cloud Console after creating a service account
# and enabling the Google Sheets API.

# The private key from the JSON file downloaded from Google Cloud Console.
# IMPORTANT: Replace newline characters (\n) in the key with actual newlines or handle them in your code.
# For Vercel/Netlify, you might need to encode it or use their specific environment variable handling.
NEXT_PUBLIC_GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT_HERE\n-----END PRIVATE KEY-----\n"

# The client email address from the JSON file.
NEXT_PUBLIC_GOOGLE_CLIENT_EMAIL="your-service-account-email@your-project-id.iam.gserviceaccount.com"

# The ID of the Google Sheet you want to read from.
# You can find this in the URL of your spreadsheet: docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
NEXT_PUBLIC_GOOGLE_SPREADSHEET_ID="YOUR_SPREADSHEET_ID_HERE"

# Optional: The name of the sheet containing the data (defaults to 'Sheet1' if not provided).
# NEXT_PUBLIC_GOOGLE_SHEET_NAME="YourSheetName"

# --- Other Environment Variables (if any) ---
# GOOGLE_GENAI_API_KEY=YOUR_API_KEY_HERE
