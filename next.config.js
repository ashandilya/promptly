/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add other Next.js configurations here if needed
  experimental: {
    // Removed allowedDevOrigins as it's not a valid option in this version's experimental config
  },
};

module.exports = nextConfig;
